model User{
    id String  @id @default(uuid())
    name String @db.VarChar(255)
    role String @default("CUSMOTER")

    email String @unique
    password String

    createdAt DateTime @default(now())
    updateAt  DateTime  @updatedAt

    @@map("users")
}